# Getting started with DPS CMS Theme

## Intro

There are 2 packages, a `core` package and a `theme` package.  
This one is the theme package.  

## Entrypoint

The main entrypoint of this template is located at `core-pkg/layouts/default.vue`.
